<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.VoiceKeyboard" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:navigationBarColor">@color/surface</item>
    </style>

    <style name="Theme.VoiceKeyboard.Dark" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:navigationBarColor">@color/surface_dark</item>
    </style>

    <style name="KeyboardTheme">
        <item name="android:background">@color/keyboard_bg</item>
    </style>

    <style name="KeyboardTheme.Dark">
        <item name="android:background">@color/keyboard_bg_dark</item>
    </style>

    <!-- Dialog theme for TranscribeActivity -->
    <style name="Theme.VoiceKeyboard.Dialog" parent="android:Theme.Material.Light.Dialog">
        <item name="android:windowMinWidthMajor">85%</item>
        <item name="android:windowMinWidthMinor">85%</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
